spring:
  application:
    name: demo

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/internship_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

seaweedfs:
  filer-url: http://localhost:8888
  document-directory: /documents
